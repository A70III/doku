# 08 — Decisions

บันทึกข้อสรุปทั้งหมด (เดิมคือ open questions) — ถ้ามีคำถามใหม่ให้เพิ่มหัวข้อ "รอเคาะ" ท้ายไฟล์

## ล็อกแล้ว

| # | เรื่อง | ข้อสรุป |
|---|---|---|
| 1 | meta sidecar | **วางข้างไฟล์** (`design.meta.json`) — Obsidian plugin ตัดออกแล้ว จึงไม่มีเหตุต้องซ่อน |
| 2 | trash retention | **auto 30 วัน** หรือคนกดเคลียร์เอง (แล้วแต่ก่อน) |
| 3 | docker | เริ่มหลัง **M4 นิ่ง** |
| 4 | font | **Inter + Noto Sans Thai** (self-host) |
| 5 | default accent | น้ำเงิน **`#2b5fc4`** — เดิม `#3b7df0` แต่ contrast บนพื้นอุ่นได้ 3.87:1 (ไม่ผ่าน WCAG AA สำหรับ link) จึงเข้มขึ้นเป็น 5.9:1 · ยังเป็นน้ำเงินตัวเดียวกัน แก้ตอน UI pass ([03 §1.1](03-blocks-and-design-system.md)) |
| 6 | highlight style | **พื้นเต็มบล็อกจางสี** — full tint ทั้งช่วงข้อความ, mapped ตาม `{.color}` (`--dk-mapped-bg`), `box-decoration-break: clone` กัน mark พับหลายบรรทัดสีสลัด · เดิมเป็น **brush underline** (gradient สีเฉพาะ 42% ล่าง) แต่วัดจากการใช้จริงแล้วสีไม่เต็มข้อความจนดูพลาดพาด |
| 7 | post-it | **ลบออกทั้งหมด** — ไม่มี block นี้ในระบบ |
| 8 | zen mode + command palette | ทำใน **M3** |
| 9 | asset URL | serve `/assets/<vault-path>?h=<hash>` · upload `POST /api/docs/*path/assets` · delete `DELETE /api/assets/*path` (ดู [05](05-api-and-agent-access.md)) |
| 10 | trash location | `vault/.trash/` — watcher/tree/index ข้าม dotfolder |
| 11 | `created` | ISO 8601 **date-time** |
| 12 | JSON Schema | **generate จาก Zod** (`z.toJSONSchema()`, draft 2020-12) — ไม่ commit, สร้างตอน M0 ด้วย `bun run gen:schema` |
| 13 | folder meta schema | **ไม่แยกไฟล์** — validate ด้วย Zod object เดียวกับ meta |
| 14 | callout | มี `success` ครบ 7 type |
| 15 | figure | ใช้ `width` + `align` (`full` แทน bleed) |
| 16 | MVP | M0 + M1 + M2 |
| 17 | web editor | **CodeMirror 6** |
| 18 | Obsidian plugin | **ตัดออกจาก scope** — เปิด vault ด้วย Obsidian ได้ตรงๆ แบบอ่าน/แก้ไฟล์ ไม่มี plugin |
| 19 | diagram เริ่มต้น | Excalidraw SVG + ASCII; D2 หลัง v1; Kroki เป็น option |
| 20 | vault default | `vault/` |
| 21 | Tailwind setup | อยู่ที่ **M1** — M0 ยังไม่มี app chrome และ `doku render` ต้องไม่พึ่ง build step (preview shell ใช้ CSS layer + token) |
| 22 | node fs adapter | แยก **`packages/fs-node` (`@doku/fs-node`)** — leaf package ที่ cli/server/mcp depend ได้ · `core` ยัง isomorphic (ห้ามผูก `node:fs`) |
| 23 | KaTeX ใน preview | **ไฟล์เดียวจบ** — ฝัง woff2 ทั้งหมดเป็น data URI (~380 KB เมื่อมีสมการ) เพื่อให้เปิด offline ได้ 100% |
| 24 | การซ้อน directive | **`:::` ชั้นนอกต้องยาวกว่าชั้นใน** (เช่น `::::tabs` + `:::tab`) — `doku check` ตรวจเป็น error (`block_nesting_ambiguous`) |
| 25 | `doku serve` | **CLI spawn subprocess** — cli ห้าม import `@doku/server` ตรงๆ (ทิศทาง dependency) · ส่งค่าผ่าน env `DOKU_VAULT`/`DOKU_PORT`/`DOKU_HOST` |
| 26 | HTML cache file | **JSON envelope** ที่ `var/cache/<hash>.json` (fragment + meta + toc + warnings) — เพราะ layout ต้องใช้ meta/toc ต่อ request, เก็บเป็น `.html` ตาม docs/01 เดิมทำไม่ได้ |
| 27 | CSP | `font-src 'self' data:` — เพิ่ม `data:` สำหรับ woff2 ที่ KaTeX ฝัง (ตามข้อ 23), ส่วนอื่นตาม docs/06 เดิม · route ชื่อ `/sse` (event `change` → client reload) · `/static/*` = static ที่ server คุมเอง |
| 28 | content CSS อยู่ที่ core | `tokens` / `prose` / `blocks` อยู่ที่ `packages/core/src/styles/` (export `CONTENT_CSS`) — CLI preview กับ server ใช้ชุดเดียวกันจริง ๆ · chrome ของแต่ละหน้าอยู่ที่ผู้ใช้ (server = Tailwind, CLI = `PREVIEW_CHROME_CSS`) · เหตุผล: CLI import `@doku/server` ไม่ได้ (ทิศทาง dependency) จึงต้องมีที่เดียวใน core ไม่งั้น CSS drift (แก้ docs/03 §10) |
| 29 | block renderer คืน hast | `BlockDefinition.render(ctx)` คืน hast `Element` **ไม่ใช่ HTML string** และสไตล์ใช้ `data-*` ไม่ใช้ inline `style` — เพื่อให้ rehype-sanitize ครอบทุก node ที่ block สร้าง (ตาม invariant “sanitize เสมอ”) · ค่าที่ต้องเป็นตัวเลข (figure width, motion delay/duration, progress) ถูก validate + quantize แล้วแทนด้วย data attribute + CSS rule ที่ generate (ไม่มี inline style) |
| 30 | `==mark=={.สี}` | implement ด้วย remark plugin ที่แปลง **mdast text node** (ไม่ทำ micromark extension) — สีต้องอยู่ใน allowlist `red orange amber yellow green teal blue purple` · `:name` กลางข้อความโดยไม่มี `[...]` (เช่น `bun:sqlite`) = ข้อความธรรมดา ไม่นับเป็น directive |
| 31 | ทิศทาง UI | **Digital Archivist / Editorial Minimalism** — หน้าตามาจาก **typography · hierarchy · spacing · โครงสร้าง · ความยับยั้งชั่งใจ · จังหวะ** ไม่ใช่จากของประดับ · ห้ามของประดับเชิงสัญลักษณ์ (ชั้นหนังสือ · parchment · ornament · UI ย้อนยุคปลอม) · เป้าหมายคือ minimal ที่ **ไม่ generic** — เกณฑ์ตัดสิน: typography แก้ได้ → ไม่เพิ่มของประดับ · spacing แก้ได้ → ไม่เพิ่ม container · hierarchy แก้ได้ → ไม่เพิ่มสี (ดู [03 Part B](03-blocks-and-design-system.md)) |
| 32 | container | **card ไม่ใช่ default** — ไต่ **บันได container** whitespace → divider/hairline → typography → indentation → พื้นหลังจาง → card และหยุดที่ขั้นแรกที่พอ · ขั้น card เฉพาะเมื่อเนื้อหาเป็น object อิสระที่มีการกระทำของตัวเอง (ห้ามการ์ดซ้อนการ์ด / การ์ดต่อ 1 section / กริดการ์ดทุกหน้า) · **ยกเลิก "เนื้อหาเป็นการ์ดลอยบน `--k-app-bg`" ของ M2** — chrome แยกด้วย hairline + whitespace (ดู [03 §2](03-blocks-and-design-system.md)) |
| 33 | พื้นผิว / สี / ระยะ | **warm neutral + accent เดียว** — surface/line/text อยู่ตระกูลอุ่นเดียวกัน · ไม่ใช้ `#fff`/`#000` ล้วน · accent (default ตามข้อ 5) ใช้กับ *สถานะปัจจุบัน* เท่านั้น · semantic อิ่มต่ำ ใช้เมื่อเป็น state จริง · **radius 2/4/6px** คงที่ และ **shadow เฉพาะ overlay** (menu/modal) — depth ปกติมาจาก hairline + พื้นหลังจาง + whitespace · ค่า hex อยู่ที่ [03 §1.1](03-blocks-and-design-system.md) และ **ล็อกแล้ว** (ตรวจ contrast + `/styleguide` ตอน migrate) · migrate แล้วใน UI pass (ข้อ 36) |

| 34 | ชุดไอคอน | **Lucide** — ISC · 1,600+ ไอคอน · grid 24px · stroke ปรับได้ (ใช้ 1.5–2) · เหตุผล: [static SVG เป็น first-class](https://lucide.dev/guide/static/) (`lucide-static` — ไฟล์ `.svg` เดี่ยว / sprite / icon font ใช้ได้โดยไม่ต้องมี JS runtime) · community/maintenance ใหญ่สุดในกลุ่ม line icon · ปรับ stroke ให้บางตามทิศทาง UI ได้โดยไม่เปลี่ยนชุด · **vendor subset เข้า repo** (`packages/core/src/icons/`) ห้าม CDN / ห้ามโหลดทั้งชุด / ห้าม icon font (LAN only + CSP `default-src 'none'`) · ชื่อที่ไม่รู้จัก → ไม่แสดง + warning `icon_unknown` · ต้องเก็บ ISC notice ไว้ในไฟล์ icon module |
| 35 | การส่งไอคอนเข้า HTML | **chrome (TSX) = inline `<svg>` จาก map ใน core** (`stroke="currentColor"` รับสีจาก token) · **เนื้อหาเอกสาร = CSS `mask-image` + `background-color: currentColor`** กับ `url("data:image/svg+xml,…")` ที่ generate ต่อชื่อ icon → **ห้าม inline `<svg>` เข้า HTML ที่ผ่าน sanitize** (allowlist ไม่มี `svg`/`path` และจะไม่เพิ่ม — [06](06-security.md)) · ไม่ต้องแก้ CSP เพราะ `img-src 'self' data:` มีอยู่แล้ว (ข้อ 27) · แก้ปม `:::card{icon=…}` ที่ `data-icon` ไม่มี CSS อ่านค่า → ทำพร้อม UI pass ที่ [M3](07-roadmap.md) |
| 36 | UI pass M0–M2 | ปรับหน้าตาบน state M0+M1+M2 โดยไม่เพิ่มฟีเจอร์ — **ทำตามข้อ 31–33 ให้ครบ**: ① migrate token เป็น warm neutral ตาม [03 §1.1](03-blocks-and-design-system.md) (rail น้ำตาลอุ่น `--k-app-bg` + ใบกระดาษ `--k-bg`) · ② accent + `--d-text-subtle` ปรับให้ผ่าน AA (ข้อ 5) · ③ **แก้บั๊ก `data-theme="auto"`**: เดิมมีแต่ `[data-theme="dark"]` ไม่มี `@media (prefers-color-scheme: dark)` → โหมด auto ไม่มีวันเป็น dark · ④ **rail + ใบกระดาษ** (ไม่ใช่การ์ด): hub/catalogue ใช้ masthead + hairline + tabular figures, reading page วางคอลัมน์ชิด rail · ⑤ **ชื่อเรื่องเดียว**: h1 นำหน้าของ body เป็นชื่อเอกสาร (ตัด h1 ซ้ำ), meta `title` ที่ระบุชัดเจนชนะ (bump `RENDERER_VERSION` → 3) · ⑥ ตัด gradient ประดับของ `section(hero)` · ⑦ rail ซ่อนใต้ `md` (content ไม่ถูกบีบ) |
| 37 | editor bundling | **CodeMirror 6 bundle เป็น static artifact** — `Bun.build` จาก `packages/server/src/web/editor.ts` → `packages/server/public/editor.js` (`.gitignore`) · ต้องรัน `bun run build:editor` ก่อนเปิด server (`scripts/dev.sh` ทำให้อัตโนมัติ) · CSP `script-src 'self'` → **self-host เท่านั้น ห้าม CDN** · ไม่มี bundle = client ใช้ `<textarea>` fallback (หน้าเว็บยังใช้ได้) |
| 38 | Lucide regeneration | subset ที่ **commit** (`packages/core/src/icons/lucide.ts`) generate จาก `lucide-static` ซึ่งเป็น **devDependency** (`bun run gen:icons`) · ISC notice อยู่ในไฟล์ที่ generate · runtime ไม่มี dep กับ lucide · ชื่อไอคอนที่ไม่รู้จัก → ไม่แสดง + warning `icon_unknown` |
| 39 | trash manifest | `.trash/<ts>-<rand>/` + manifest `.doku-trash.json` = `{id,label,kind,sources,deletedAt,bytes}` · **1 รายการ = 1 โฟลเดอร์** (doc = md + meta ไปด้วยกัน) · กู้คืน = ย้าย `sources` กลับ path เดิม (ถ้าซ้ำ = 409 ไม่ทับ) · id ต้องผ่าน `isTrashId` ก่อนแตะ fs |
| 40 | concurrency status codes | PUT/PATCH **ไม่ส่ง** `If-Match` → `428 precondition_required` · **ไม่ตรง** → `409 conflict` + ส่ง ETag ปัจจุบันกลับ · รับ `If-Match: *` และ weak prefix `W/` (RFC 9110 แบบย่อ) · path safety ผ่านหมดก่อนแตะ fs |
| 41 | move API | `POST /api/docs/*path/move` + `POST /api/folders/*path/move` รับ `{to, update_links?}` (`update_links` default **true**) · อัปเดตลิงก์ทั้ง vault (wikilink path form · `/d/…` · relative) + เขียน `relations.moved_from` · wikilink basename ที่กำกวม = **ไม่แตะ** (ให้ `doku check` เตือน) |
| 42 | static asset caching | `/static/app.css` + `/static/editor.js` ตอบ `ETag` และรองรับ `If-None-Match` → `304` (editor bundle ~510KB ไม่ถูกโหลดซ้ำ) · `editor.js` cache ใน memory (build ครั้งเดียว) · `app.css` อ่านใหม่ทุก request เพราะ Tailwind `--watch` |
| 43 | folder tree + color | tree เดินจาก **filesystem** ไม่ใช่รายการเอกสาร → โฟลเดอร์ว่างปรากฏใน sidebar ทันที (M3) · `_folder.meta.json.color` (ผ่าน Zod `#rrggbb`) ใช้ tint ไอคอนโฟลเดอร์ · ค่าที่ไม่ผ่าน = ไม่ใช้ (ไม่Throw) |
| 44 | cache key | `sha256(path id + md + meta + metaSource + warnings + rendererVersion + theme + listingHash)` — **path id ต้องอยู่ใน key** เพราะ render ขึ้นกับตำแหน่งเอกสาร (relative link/asset) · metaSource + warnings เปลี่ยน HTML (ตัด h1 / warnings banner) โดยไม่เปลี่ยน meta ที่ parse แล้ว · แก้บั๊ก M1–M2 ที่เอกสารต่างโฟลเดอร์ hash ชนกัน (อัปเดต docs/01 §Caching) |
| 45 | path safety ของ write/move/trash | `safeJoin` ต้องเช็ค **realpath ของบรรพบุรุษที่มีอยู่จริงลึกสุด** ไม่ใช่แค่ `realpath(target)` (target ใหม่ยังไม่มี → ENOENT → เดิมข้ามการเช็ค = symlink dir หลุด vault ได้) · trash store ใช้ guard ตัวเดียวกัน (เดิม prefix-only) · `restore` ห้ามทับ path เดิม **ที่ชั้น adapter ด้วย** (ไม่พึ่ง API อย่างเดียว) · `ENOTDIR` = "ไม่มีไฟล์" (คืน null/false ไม่ throw) |
| 46 | asset route | `/assets/*` **ห้าม strip ชื่อ vault** (path นี้ server generate เอง — docs/08 ข้อ 9) · extension ต้องมาจาก basename และต้องมี `.` จริง (ไฟล์ชื่อ `png` ไม่กลายเป็น `image/png`) · `/static/*` เทียบ `If-None-Match` แบบ weak/list ผ่าน `matchesIfMatch` (RFC 9110) · badge trash อ่านสด (ไม่ cache 2 วิ) |
| 47 | **จังหวะ + คู่สี (M3.1)** | ปรับค่าสีทั้งชุดเป็น **OKLCH** โดยใช้ **L เท่ากันทุก hue ต่อธีม** แล้ว **verify contrast เป็นคู่จริง** (รวม `color-mix` composite) ไม่ตรวจจด้วยตา — เพราะตรวจแล้วเจอ 4 คลาสตก AA: ①**เส้นขอบคอนโทรล 1.35:1** (ผิด WCAG 1.4.11) → เพิ่ม **`--d-border-control`** (light `#8d8984` / dark `#6c6964`, ≥ 3:1) แยกจาก hairline ประดับ ②**ข้อความ palette บน tint ของตัวเอง** (badge 4.14–4.47) → เพิ่ม **`--k-<name>-ink`** = `color-mix(in oklab, var(--k-X) 78%, var(--k-text))` (≥ 5.77 ทุกคู่) ③**semantic บน `--d-bg-subtle`/`bg-muted`** (3.87–4.48) → แก้ค่าราก ④`--d-text-subtle` บน `bg-muted` 4.33 → 4.58 · **semantic = alias ของ palette** (ไมใช่ชุดที่สอง) · `--d-accent` light คง `#2b5fc4` (ข้อ 5 ยังใช้ได้) แต่ **dark เปลี่ยน `#58a6ff` → `#5b93e0`** (ค่าเดิม chroma .152 อ่านเป็นนีออนเย็นบนพื้นน้ำตาลอุ่น) · **ห้าม `#fff`/`#000` ดิบ** → `--k-on-accent` / `--k-scrim` / `--k-on-scrim` · `--d-selection` = accent 24–30% (เดิม 10% แทบมองไม่เห็น) · **บังคับด้วย test** `packages/core/src/styles/contrast.test.ts` (อ่านค่าจาก `tokens.ts` ตรง ๆ — แก้สีไม่ผ่าน test = แดง) |
| 48 | **สองสเกลตัวอักษร** | แยก **chrome scale** (rail/toolbar/panel — คงเดิม) จาก **reading scale** (`--d-read*`: body 1.0625rem · h1 clamp(2–2.75) · h2 clamp(1.5–1.75) · h3 1.25 · h4 = body) · **weight ใช้แค่ 400 + 600** (ตัด 500/700) · `--k-measure` 68ch → **70ch** · `--k-leading-body` เดิม define 1.75 แต่ **ไม่มี selector ไหนใช้** → ผูกเข้ากับ `.doku-prose` จริง และ `:lang(th)` ใช้ 1.9 · เหตุผล: สเกลเดิมทำให้ h1 36px → h2 30px (อัตราส่วน 1.2) ซึ่งแคบจนสองชั้นดูเท่ากัน |
| 49 | **TOC + colophon** | **TOC ออกจากบทความ** → sticky column 208px (≥ 1200px) · จอแคบ = แผ่น (sheet) เปิดจากปุ่มใน toolbar (เปลี่ยนจาก `<details>` ระหว่างทำจริง — ย้ายตำแหน่ง DOM ด้วย CSS ไม่ได้) · active ด้วย `IntersectionObserver` · **colophon** (path id · แก้ไขล่าสุด · revision) ย้ายจาก `doku-shelfmark` เหนือ h1 → **ท้ายเอกสาร** · เหตุผล: วัดจากหน้าเว็บจริง TOC inline สูง **352px = 39% ของ viewport** และ mono label เหนือ h1 แย่งความสนใจชื่อเรื่อง (ขัด docs/03 §2 ที่เขียนไว้ตั้งแต่ต้นว่าอยู่ sidebar) |
| 50 | **ห้ามอ้าง `--d-space-*` ที่ไม่ถูก define** | เจอแล้วว่า `app.css:88,477` อ้าง `--d-space-5` ซึ่งไม่มีใน `tokens.ts` → CSS ทิ้ง declaration ทั้งก้อน → **rail และ panel ทุกตัว `padding: 0`** (ยืนยันด้วย computed style) · แก้: เพิ่มขั้นที่ขาด (`5/10/16/20/24`) + `tokens.test.ts` ตรวจว่า `var(--d-space-N)` ทุกตัวมี definition ของจริง · กฎเดียวกับ `--d-*` อื่นทุกตัว |
| 51 | **toolbar ถูก demote** | เดิมเป็นแถวปุ่ม 5 อัน (แก้ไข/คุณสมบัติ/ประวัติ/ย้าย/ลบ) **เหนือชื่อเรื่อง** → **ไม่มีปุ่ม "แก้ไข" เลย** (เอกสารพิมพ์ได้ทันที — ข้อ 52) เหลือปุ่ม `⋯` (คุณสมบัติ · ประวัติ · ย้าย · ลบ) + ปุ่ม zen ที่มุมขวา · `ลบ` เข้าเมนูพร้อมยืนยัน · เหตุผล: 5 ปุ่มน้ำหนักเท่ากัน = ไม่มีลำดับชั้นของ controls และชื่อเรื่องกลายเป็นของแถม · **การมีปุ่ม "แก้ไข" เป็นการบอกโดยอ้อมว่าเอกสารนี้อ่านได้อย่างเดียว ซึ่งไม่ตรงกับความจริงของ doku** |
| 52 | **โมเดลการเขียน = Live Preview ในที่ (ไม่มีโหมด)** | **ยกเลิก overlay + split pane** (`pages.tsx` `.doku-editor`) **และยกเลิกปุ่ม/โหมดแก้ไขทั้งหมด** — หน้าเอกสารที่เปิดอยู่ **เป็น editor ตั้งแต่แรก**: ไม่มี `doku-editor` overlay, ไม่มี `openEditor()`, ไม่มี dirty chip/save button · ใช้ **CodeMirror 6** (ข้อ 17) เป็นผิวเดียวกับหน้าอ่าน — คลิกที่ไหน カーไปที่นั่น พิมพ์ได้ทันที · ความกว้าง/ฟอนต์/leading เท่ากับตอนอ่านเป๊ะ → **ไม่มี mode switch** และไม่มี state ให้ desync · syntax marker ซ่อนเมื่อカーอยู่นอก node (`ViewPlugin` + `Decoration.replace/mark`) · รูป `![](...)` เป็น **widget ในบรรทัด** · node อื่น (table · code · math · callout) ได้ decoration **ระดับบรรทัด** ในรอบนี้ (widget เต็มรอรอบถัดไป — attribute แก้ผ่าน block control strip) · **ไม่เพิ่ม engine ใหม่** (ไม่ Tiptap/ProseMirror) · ประกาศ `@codemirror/autocomplete` เป็น direct dep (มีใน lockfile แล้วผ่าน `lang-markdown` — ไม่ download ใหม่) · fallback `<textarea>` เมื่อไม่มี bundle (ข้อ 37) ยังต้องใช้ได้ · **หัวเอกสาร (ชื่อเรื่อง/สรุป/meta) ต้องอยู่นอก fragment** ที่ editor แทนที่ — ไม่งั้นชื่อเรื่องหายขณะพิมพ์ (bump `RENDERER_VERSION` = 6) · **SSE live-reload ต้องถูกกันระหว่างแก้** (`html[data-editing]`) ไม่งั้น autosave ของเราเอง trigger watcher แล้วหน้า reload กลางการพิมพ์ |
| 53 | **เก็บกรอบในเนื้อหา** | ตรวจแล้วเอกสารตัวอย่าง 1 ไฟล์มี **37 block ที่มีกรอบ/พื้นหลัง** → หน้าเว็บอ่านเป็น "กริดของกล่อง" ขัดข้อ 32 · ตัด container ของ `section` · `stats`/`stat` · `grid`/`col` · shadow ของ `card` · เปลี่ยน `kv` เป็น definition list · `steps`/`timeline` เหลือเส้นเดียว · `details`/`tabs` เหลือ hairline คั่น header · **คงไว้**: `callout` (ความหมาย) · `code` `figure` `gallery` `table` `video` (คนละชนิดข้อมูล) · เกณฑ์: **ถ้าลบกรอบออกแล้วข้อมูลยังอ่านรู้เรื่อง → ลบ** |
| 54 | **autosave + คีย์ลัดตาม focus** | autosave ตาม debounce (800ms หลังหยุดพิมพ์) ทำงาน**ตลอดเวลา** เพราะไม่มีโหมดแก้ไข · ใช้ `PUT /api/docs/*path` + `If-Match` เหมือนเดิม → 409 = โชว์ ETag ใหม่ให้เลือก (ไม่ทับเงียบ) · revision ต่อ save (หมุน 20/doc ตามเดิม) แต่ **ไม่สร้าง revision ใหม่ถ้าเนื้อหาเท่าเดิม** (compare hash) · ออกจากเอกสาร = คลิก chrome ที่มีความหมาย (sidebar/rail · TOC คอลัมน์+แผ่น · เมนู) หรือ `Esc` → flush ทันที · **คลิกเยื้องออกจากคอลัมน์นิดเดียวไม่ออก**: วงใน = tolerance zone รอบ `article` ขยาย ~3rem (วัดด้วยพิกัด event กับ rect — ไม่ใช่ `contains`) → ส่ง focus กลับ editor แทน · วงกลาง = พื้นหลังเปล่า (neutral gutter) → อยู่ต่อ · **คีย์ลัดขึ้นกับ focus ไม่ใช่โหมด**: カー**อยู่นอก**เอกสาร → `/` search · `t` · `z` · `d` · `?` ทำงานตามปกติ · カー**อยู่ใน**เอกสาร → `/` = slash menu, `Mod+B/I/K` = markdown, `Esc` = ออกจากเอกสาร (カーหลุด → ชุดแรกกลับมา) · ต้องมี cue ให้เห็นว่าตอนนี้カーอยู่/ไม่อยู่ (block strip + caret) · `Mod+S` = flush อย่างเดียว (ไม่มี save button) |
| 55 | **slash menu + block control strip** | พิมพ์ `/` ที่ต้นบรรทัดว่าง → เมนู block (กรองได้ พิมพ์ต่อเพื่อค้น) → แทรก **template directive** จริง (`::::tabs` + `:::tab` เมื่อต้องซ้อน — ตามข้อ 24) · カーเข้า `:::` ใด → แถบควบคุมลอยหน้าบรรทัดแรก: ชนิด (variant) · `color=` · `title=` · align/width (figure) · icon · ลบ block · **ค่าที่แก้ถูกเขียนกลับเป็นข้อความ directive ใน markdown เสมอ** — ไม่มี state ซ่อน, diff ได้ทุกไบต์ · รายการ attribute อ่านจาก block registry (`/api/schema`) → ไม่มีลิสต์ที่ต้องดูแลสองที่ · ครอบทุก block ที่มี attribute ใน M3.1 · **แผงไม่ผูกกับ focus ของ CM** — editor รายงานตำแหน่ง directive จาก selection เสมอ (ห้ามกรองด้วย `hasFocus`) · client เป็นผู้ตัดสินซ่อน: pin ระหว่างโต้ตอบ (focus/pointer อยู่ในแผง), ซ่อนเมื่อカーออกนอก directive + CM มี focus, คลิกนอกทั้ง CM และแผง (วัดพิกัด), หรือออกจากโหมดเขียน · **ไม่ rebuild DOM ระหว่าง interaction** — rebuild เฉพาะเปลี่ยน block (ชื่อ/บรรทัด fence ต่างกัน) หรือเปิดครั้งแรก, ค่า control sync ในที่ · text input แก้แบบ live (debounce ~300ms), select ใช้ `change` · **mark (`==…==`) ตั้งค่าสีผ่านแถบ swatch เมื่อカーอยู่ในช่วง** — 8 swatch ตาม allowlist + ปุ่ม “ไม่ระบุสี” · ค่าที่เขียนกลับเป็น `{.color}` ใน markdown เสมอ (ลบสี = ตัด suffix ออกทั้งก้อน) · mark อยู่ “ใน” directive ได้ → เมื่อカーอยู่ใน mark ทั้งคู่ active ให้ mark bar ชนะ (ตัวเฉพาะจุดกว่า) — reuse กลไก pin/ไม่-rebuild ของ block strip |
| 56 | `#` ในชื่อไฟล์ | **ห้าม `#` ใน vault path** — เพิ่มเข้า `FORBIDDEN_CHARS` (`paths.ts:20`) · และ **แยกฟังก์ชัน**: `normalizeVaultPath` (path จาก URL/API — **ไม่ตัด `#`**) กับ `normalizeLinkTarget` (จาก wikilink/ข้อความคน — ตัด `#<anchor>` ตามเดิม) · เหตุผล: HTTP แยก fragment ให้แล้ว การตัดอีกชั้นทำให้ **เสิร์ฟเอกสารผิดไฟล์เงียบ ๆ** (`/d/a%23b` → เปิด `a.md`) — ตรวจแล้วไม่มีไฟล์ที่มี `#` ใน `examples/vault` และ `vault` → ไม่กระทบของเดิม |
| 57 | sanitize protocol + attribute | `href` รับ **`http` `https` `mailto` `tel`** (relative/`#` ผ่านอยู่แล้ว) · `src` รับ `http` `https` · **`data:` ห้ามทั้งคู่** · **scheme ถูก normalize เป็น lowercase ก่อนเทียบ** → `HTTP://`, `MailTo:` ใช้ได้ (ยืนยันด้วย render จริง: ปัจจุบันลิงก์ถูกถอดทั้งอัน) · `a`: `target` รับเฉพาะ `_blank`/`_self` · `rel` รับ token จาก allowlist และ **renderer เติม `rel="noopener noreferrer"` เองเมื่อ `target=_blank`** (ไม่ต้องให้ md เขียน) · **ถอด `color` ออกจาก global allowlist** (`sanitize.ts:98` รับ `color` มาจาก default schema) — สีของ block มาจาก `color=` → `data-*` → CSS (ข้อ 29) ไม่ใช่ attribute `color` ตาม docs/06 |
| 58 | asset `?h=` | **verify `h` ก่อนให้ `immutable`** — `?h=` ตรงกับ hash เนื้อไฟล์จริง → `public, max-age=31536000, immutable` · ไม่ตรง/ไม่มี → `ETag` + `no-cache` (304) · hash คำนวณแบบ cache by (mtime, size) · เหตุผล: ปัจจุบัน `?h=FAKE` ได้ immutable 1 ปี (`app.tsx:314`) → แก้รูปแล้วเบราว์เซอร์ยังเห็นรูปเก่าถาวร (ลิงก์ที่พิมพ์มือใน md ไม่ได้อัปเดต hash เอง) |
| 59 | `width=70%` | **รับทั้ง `70` และ `70%`** (เทียบเท่ากัน · 5–100 สเต็ป 5) — implement แล้วที่ `blocks/figure.ts:33` · docs/03 คงเขียน `%` ได้ · ค่านอกช่วง → `block_attribute_unknown` + ไม่ใส่ data attribute |
| 60 | `meta.render.math=false` | **คงข้อความต้นฉบับ `$…$` / `$$…$$` ไว้เป็น text** + warning `math_disabled` (info) · **ห้ามตัด delimiter แล้วเหลือแค่ code** · เหตุผล: หลัก "ไฟล์คือความจริง — ปิดฟีเจอร์ไม่ทำข้อมูลหาย" · ตอน implement ต้อง **ข้าม `remark-math` ตั้งแต่ต้น** (`render.ts:136` ใส่ปลั๊กอินแบบมีเงื่อนไข) ไม่ใช่ปิดแค่ `rehype-katex` |
| 61 | wikilink หาไม่เจอ | **render ข้อความต้นฉบับเป๊ะ ๆ** — `[[target]]` → `[[target]]` · `[[target\|alias]]` → `[[target\|alias]]` + warning `wikilink_missing` · ปัจจุบัน fallback ของเคสมี alias คือ `target\|alias` (ไม่มี `[[ ]]` — `plugins/wikilink.ts:82`) → ไม่สอดคล้องกับเคสไม่มี alias และกลืนข้อมูล |
| 62 | **แผงควบคุม block ไม่ผูกกับ focus ของ CM** | **editor รายงานตำแหน่ง directive เสมอ** — `updateListener` ส่ง `directiveAtCursor(view)` ทุกครั้งที่ `selectionSet`/`docChanged`/`focusChanged` **ไม่กรองด้วย `hasFocus`** (เดิม: คลิกที่แผง → CM เสีย focus → ส่ง null → แผงหาย + rebuild DOM ทุก update → dropdown ที่เปิดค้างปิดตัวเอง) · **client เป็นผู้ตัดสินซ่อน** — pin ระหว่างโต้ตอบ (focus/pointer อยู่ในแผง = ห้ามซ่อน), ซ่อนเฉพาะ: (a) カーย้ายไปบรรทัดนอก directive และ CM มี focus, (b) pointerdown นอกทั้ง CM และแผง (**วัดด้วยพิกัด event กับ `getBoundingClientRect` ของแผง + CM host — ไม่ใช้ `contains` อย่างเดียว** เพราะ element ถูกแทนที่ระหว่างคลิก), (c) ออกจากโหมดเขียน · **ไม่ rebuild DOM ระหว่าง interaction** — rebuild เฉพาะเปลี่ยน block (ชื่อ/บรรทัด fence ต่างกัน) หรือเปิดครั้งแรก, ค่า control sync ในที่ (ข้าม control ที่กำลัง focus) · text input แก้แบบ live — dispatch patch ทุกการพิมพ์ debounce ~300ms · ตำแหน่งแผงคำนวณใหม่เมื่อ scroll/resize (delta ของ editor host) · หลัง `patchDirective` จากแผง บรรทัด fence เปลี่ยน → editor ต้องรายงาน info ใหม่หลัง `docChanged` |
| 63 | **block interaction ต้องติดตั้งซ้ำได้หลัง DOM ถูกวาดใหม่** | เจอบั๊กจริง: เข้าโหมดเขียนแล้วออกมาโดยไม่เปลี่ยนหน้า → `:::tabs` **กดสลับแท็บไม่ได้** (เกิดกับ copy code / figure zoom / motion reveal ด้วย) · root cause: `INTERACTIONS_JS` (`packages/core/src/client.ts`) เป็น statement ที่รัน **ครั้งเดียวตอนโหลดหน้า** และ `addEventListener` กับปุ่มแท็บ**ตัว node** ขณะที่การออกจากโหมดเขียน repaint in-place ด้วย `bodyEl.innerHTML = result.html` (ข้อ 52/54) → node เดิมตายทั้งยวง: handler หาย, `data-enhanced` หาย (panel ทุกอันกลับมาแสดงพร้อมกัน), ปุ่ม copy หาย, motion block ค้าง `opacity: 0` เพราะ observer ไม่รู้จัก node ใหม่ · **กติกา 2 ข้อ**: ① interaction ที่ไม่ผูกกับ node (tabs · figure zoom) → **event delegation ที่ `document`** (ห้าม `addEventListener` บนตัว block node) ② ของที่ต้องติดตั้งบน DOM (data-enhanced · ปุ่ม copy · observer ของ motion/TOC) → รวมใน `enhanceContent(root)` ที่ **idempotent** และ expose เป็น `window.DokuInteractions` ให้ฝั่ง server เรียกหลังวาดใหม่ทุกครั้ง (CLI preview ใช้สคริปต์ตัวเดียวกัน) · เกณฑ์ตัดสิน: ตราบใดที่ยังยอมให้ repaint เนื้อหา in-place ห้ามมี interaction ที่พึ่ง "รันครั้งเดียวตอนโหลด" · regression test: `packages/server/test/tabs-after-paint.test.ts` (execute `INTERACTIONS_JS` กับ DOM จริงจาก pipeline ผ่าน linkedom) |

| 64 | **สลับ read ↔ write แล้ว layout ต้องอยู่นิ่ง (rail ห้ามเด้ง) + "บรรทัดเดิม" ต้องอยู่ที่เดิม** | รายงานผู้ใช้: เข้า/ออกโหมดเขียนแล้วเด้งไปเด้งมา · วัดจากเบราว์เซอร์จริงก่อนแก้: ① ระหว่างสลับ DOM (ล้าง `#doku-doc-body` ตอนเข้า · ลบ editor host ตอนออก) คอลัมน์อ่านสูงเป็น 0 ชั่วขณะ → `scrollHeight` เท่า viewport → **เบราว์เซอร์ clamp `scrollY` เป็น 0** (วัดได้ 1200 → 0) และ scrollbar ถูกถอด–ใส่กลับ → shell ที่ `margin-inline: auto` เลื่อนซ้าย-ขวา = rail เด้ง · ② prose กับ editor ใช้ rhythm ต่างกัน → เอกสารยาวไม่เท่ากัน (วัดจริง 5362px vs 4031px) → กลับมาแล้วบรรทัดที่คลิกเพี้ยน **1185px / 1200px** · **กติกา**: (ก) ล็อกความสูงคอลัมน์อ่านเดิม (`min-height`) ไว้ตลอดการสลับ แล้วปล่อยเมื่อเนื้อหาใหม่ถูกวาด/วัดเสร็จ เพื่อไม่ให้ document คร่อมเส้น scrollable · (ข) คืน `scrollY` แบบ `behavior: "instant"` — `<html>` ตั้ง `scroll-behavior: smooth` ไว้ (docs/03 §7) ถ้าใช้ `scrollTo` ธรรมดาจะกลายเป็นเลื่อนยาวให้เห็น · (ค) ยึด **"บรรทัด" ไม่ใช่ตัวเลข scrollY**: จำข้อความ + ตำแหน่งบนจอของบรรทัดนั้น แล้วจัดบรรทัดเดียวกันให้อยู่ที่เดิม (เข้า = block ที่คลิก · ออก = บรรทัดที่カーอยู่ ถ้าカーไม่อยู่ในจอ → บรรทัดบนสุดที่มองเห็น) · (ง) `html { scrollbar-gutter: stable }` กัน scrollbar โผล่/หาย · **บทเรียนที่วัดแล้ว**: CM วัด layout เป็นระยะ (บรรทัดที่ยังไม่วัดใช้ค่า estimate) → จัดครั้งเดียวแล้วเลื่อนหลุดอีก ~350px จึงต้องวน align ใน rAF จน "นิ่งจริง" (≥8 เฟรม + residual ≤ 1 ติดกัน 4 เฟรม, เพดาน 45 เฟรม) และหยุดทันทีเมื่อ ผู้ใช้เลื่อนเอง (wheel/touch/key) หรือออกจากโหมดเขียน · ห้ามเรียง candidate ตามระยะบนจอ (ตอนยังไม่วัด layout = เลือกมั่ว) ให้เรียงตามความยาวข้อความ → ความลึกของ node · **บั๊กที่เจอระหว่างทาง**: `offsetForElement` หา block ใน markdown ไม่เจอเมื่อหัวข้อมี decorative anchor `a.doku-anchor` ("หัวข้อ 6#") หรือ block ขึ้นต้นด้วย inline markup → カーตกไปต้นไฟล์ทุกครั้ง · แก้: ถอด `.doku-anchor` ก่อนอ่านข้อความ + ลดความยาว prefix (เต็ม → 32 → 24 → 16 → 12) · ผลหลังแก้ (วัด 8 ตำแหน่ง scroll × p/h2/li): เพี้ยน 0–1px, ไม่มีเฟรมที่ document ยุบตัว, rail/clientWidth คงที่ · regression: `packages/server/test/mode-switch-layout.test.ts` |
| 65 | **`:::video` = `src` เดียว (assets หรือ YouTube) + poster มาจาก src** | เคาะหลังเจอบั๊กแผงควบคุม block ล้นกรอบ (rail/ชื่อง block ถูกบีบ — แก้ด้วย flex-wrap ที่ข้อนี้) · **ข้อเท็จจริงก่อนเคาะ**: `poster`/`loop`/`muted` **มีผลจริง** มาก่อน (renderer อ่านค่าจาก directive) · ที่ "ดูเหมือนไม่มีผล" เพราะแผงวาด flag เป็น text input ที่ต้องพิมพ์ `true`/`1`/`yes` ถึงจะติด (ค่าอื่น = false เงียบ ๆ) · `controls` เท่านั้นที่ไม่มีผล (renderer บังคับ `controls: true` เสมอ) · **ถอดออกทั้งหมด** — block เหลือ `attributes: ["src"]`: ไม่มี `poster` (derive จาก `src`: หาไฟล์รูป basename เดียวกัน `.png .jpg .jpeg .webp .avif` ในโฟลเดอร์เดียวกับวิดีโอ ผ่าน `rehypeRewrite` — เพราะ block renderer ไม่มี fs, core เป็น isomorphic) · ไม่มี `loop`/`muted`/`controls` (controls เปิดเสมอ) · audio ไม่มี poster · **src รับลิงก์ภายนอกได้ เฉพาะ YouTube** (watch?v= / youtu.be / shorts / embed / live) → `<iframe>` จาก `www.youtube-nocookie.com/embed/<id>` ที่ renderer ประกอบ URL เอง (ไฟล์ภายนอกอื่นยังไม่รองรับ เพราะ CSP `media-src 'self'`) · **ผลต่อความปลอดภัย**: เพิ่ม `iframe` เข้า sanitize allowlist + CSP เปิด `frame-src https://www.youtube-nocookie.com` เท่านั้น และ `rehypeRewrite` (หลัง sanitize) **ถอด iframe ทุกตัวที่ src ไม่ตรงรูปแบบนี้** เป็นด่านสุดท้าย (docs/06) · **breaking**: เอกสารเดิมที่เขียน `poster=`/`loop`/`muted` จะได้ warning `block_attribute_unknown` (info) และ attribute นั้นไม่มีผล — แต่ poster ยังได้จากไฟล์ข้าง wิดีโอถ้ามี |
| 66 | **เขียน fence ของ directive กลับ = คง attribute เดิมทั้งหมด** | เจอบั๊กจริงตอนตรวจแผงควบคุม: `parseAttrs` ใช้ `/([\w-]+)\s*=\s*"([^"]*)"/g` → **อ่านได้เฉพาะค่าใน double quote** ส่วน `src=assets/a.png` (ไม่มี quote — รูปแบบที่ docs/03 และเอกสารจริงใช้) และ flag เปล่า (`zoom`) ถูกมองข้าม → พอ `writeAttrs` สร้าง fence ใหม่จากชุดที่อ่านได้ **attribute ที่อ่านไม่ได้หายจากไฟล์ทั้งหมด** (ทดสอบ: `:::figure{src=assets/a.png caption="A" zoom}` + แก้ `align` → ได้ `:::figure{align="center" caption="A"}` = รูปหายทั้งรูป) และแผงก็โชว์ช่อง `src` ว่างด้วย · **แก้**: `parseAttrs` รับทุกรูปแบบที่ directive ยอมรับ (`key="v"` · `key='v'` · `key=v` · `key` เปล่า) · `writeAttrs` — `null` = สั่งลบเท่านั้น ส่วนค่า `""` เขียนกลับเป็น **flag เปล่า** (ไม่ลบ) เพื่อไม่ให้ attribute ที่ผู้ใช้ไม่ได้แก้หาย · `parseAttrs`/`writeAttrs` export ให้เทสได้ + `window.DokuEditor` ผูกเฉพาะเมื่อมี `window` (import ได้ใน bun test) · regression: `packages/server/test/editor-attrs.test.ts` |
| 67 | **block ต้องไม่ทำเนื้อหาผู้เขียนหาย (bug-hunt M3.1)** | เจอ 3 บัคจากการ sweep `packages/core/src/blocks/*.ts` — ① `::::tabs` จับคู่ `:::tab` (mdast) กับ **element ทุกตัว** (hast) ด้วย index → ย่อหน้า/block อื่นที่แทรกอยู่เลื่อนคิว: **tab หายทั้งก้อน + panel ติด `aria-label` ผิดคน** (ยืนยันด้วย render จริง: `tab{A}` + ข้อความกลาง + `tab{B}` → `BBB` หายทั้งก้อน และ panel ที่ 2 มีเนื้อหาเป็นข้อความกลาง) · **แก้**: กรองฝั่ง hast ด้วย `data-block="tab"` เท่านั้น และ **เนื้อหาที่ไม่ใช่ tab ถูกย้ายไปท้ายบล็อก + warning `block_stray_child`** (ห้ามทิ้ง — หลักเดียวกับข้อ 60/61 "ปิด/ผิดไม่ทำข้อมูลหาย") ② `:::card` เช็ค `href.endsWith(".md")` จากสตริงทั้งก้อน → `href="design.md#top"` พลาดเงื่อนไข ตกไป branch path id → `/d/design.md#top` = **ผิดเอกสาร + `.md` ติดใน URL** (เทียบ `[x](design.md#top)` ที่ resolve relative ถูกเป็น `/d/sub/design#top`) · **แก้**: ตัดส่วน `#`/`?` ออกก่อนเทียบ ③ `:::motion` เทียบ `once === "false"` เอง → **กลับด้าน**: `once=0` / `once=no` / `once=False` กลายเป็น true · **แก้**: ใช้ `flag()` กลาง (`blocks/types.ts`) โดยคง "ไม่ระบุ = true" เป็น default ของ block · เพิ่ม `block_stray_child` เข้า `WarningCode` · regression: `packages/core/test/blocks.test.ts` |
| 68 | **block ที่ไม่ใช้เนื้อในต้องไม่ทิ้งเนื้อหา + attribute ผิดต้องไม่ทำ block หาย** | ต่อจากข้อ 67 (sweep `blocks/*.ts` รอบสอง) — ① `:::figure` · `:::video` · `:::progress` · `:::section{type=divider}` **ไม่เคยใช้ `ctx.children` เลย** → เนื้อในที่เขียนมาหายเงียบ · **แก้**ด้วย helper กลาง `warnStrayChildren()`/`strayChildren()` (`blocks/types.ts`): แสดงเนื้อในต่อท้าย block + warning `block_stray_child` · `:::section{type=divider}` เปลี่ยนจาก `hr[data-block=section]` เป็นตัวห่อ `div[data-block=section][data-variant=divider] > hr` เพื่อให้มีที่วางเนื้อในได้ · `:::video` ที่มีเนื้อในห่อ `figure` แล้วต่อท้าย (media element เก็บ children ไม่ได้) ② `:::progress{value=abc}` เดิม **คืน `null` = block + เนื้อในหายทั้งก้อน** → ไม่มีแถบ แต่คง label/เนื้อใน + ข้อความบอกเหตุแทน + warning เสียงเดิม ③ `:::video` ไม่มี src / src ภายนอกที่ไม่ใช่ YouTube เดิมใช้ `data-block="figure"` **กล่อง dashed เปล่า ไม่มีข้อความ** → ใช้ `data-block="video" data-missing` + `figcaption` บอกเหตุ (ไม่ echo URL ที่ถูกปฏิเสธ — warning ยังมี) + CSS dashed ใช้ร่วมกัน ④ `:::card` ไม่มี `href` เดิมเป็น `<a>` ที่คลิกไม่ได้ → render เป็น `<div>` (CSS เลิกบังคับ tag `a`; hover ยังอยู่เฉพาะลิงก์) · regression: `packages/core/test/blocks.test.ts` |
| 69 | **prose rhythm — เจ้าของระยะแนวตั้งเพียงตัวเดียว** | เจอบั๊กจริง: **ย่อหน้าในเอกสารไม่มีย่อหน้า** — ระยะระหว่าง block ที่ออกแบบไว้ (`--d-flow` 24px · `--d-flow-loose` 32px) **ไม่เคยถูกใช้เลยตั้งแต่ M1** · root cause: `.doku-prose > * + *` มี specificity (0,1,0) เพราะ `*` ไม่เพิ่ม specificity → **แพ้** `.doku-prose p { margin: 0 }` (0,1,1) และ `margin: 0` เป็น shorthand ที่ reset `margin-top` ไปด้วย (วัดจากเบราว์เซอร์จริง: `<p>` ทุกตัว `margin-top: 0px` · top ต่างกัน 1 line box) — เหยื่อเดียวกัน: `pre` · `blockquote` · `kv` · `callout` · `figure` · `timeline` · `margin-note` ที่ตั้ง `margin: 0` ไว้เอง ทั้งที่ docs/03 §1.4 เขียนไว้ว่า "ห้ามตั้ง margin เดี่ยว" · ชั้นที่สอง: `> * + *` match เฉพาะลูกโดยตรงของ `.doku-prose` → ย่อหน้าใน `section` · `blockquote` · `li` ไม่มีระยะเลยไม่ว่า specificity จะเป็นเท่าไร · **เคาะกลไก 3 ชั้น**: ① ล้าง UA margin ด้วย `:where()` (specificity 0) ② flow rule ใช้ `:not(:first-child)` (0,2,1) + `:not(:is(h1…h6, hr))` (heading/`hr` มี `--d-rhythm-*` ของตัวเอง) ครอบ container ครบ (prose · `section` · `blockquote` · `col` · `tab-panel`) · **ลูกที่ตามหลัง heading/`hr` ไม่รับ flow** — ระยะหลัง heading มาจาก `--d-rhythm-after` ตัวเดียว (ถ้าปล่อย margin ของลูก collapse กับ margin ของ heading → 24px แทน 12px = อ่านเหมือนเว้นบรรทัดเกิน · เจอจากการใช้งานจริง) · container แน่น (card-body · callout · details · margin-note) = `--d-space-2` · `li` = `--d-space-2` (nested list คง `--d-space-1`) · block หนัก (pre · table · figure · gallery · callout) = `--d-flow-loose` → ปิด drift ที่ token ถูก define ไว้แต่ไม่เคยถูกใช้ ③ element ที่ต้องจัดระยะเอง (โน้ตข้างที่ลอย ≥1400px · figure zoom · print) ตั้ง **`--dk-flow`** ที่ตัวเอง — ไม่ต้องสู้ specificity · **ห้ามตั้ง margin แนวตั้งใน element rule อีก** · guard 2 ชั้น: `packages/core/src/styles/rhythm.test.ts` (คำนวณ specificity จริง — จำลองบั๊กเดิมแล้ว fail 3 ข้อ) + rhythm check ใน `bun run shot` (วัด computed margin ของลูกที่ไม่ใช่ตัวแรกในทุก flow container) |
| 70 | **katex.css โหลดเฉพาะหน้าที่มีสมการ** | เจอบั๊กจริง: **หน้าอ่านคณิตศาสตร์ไม่มี katex.css เลย** — route `/static/katex.css` + `loadKatexCss()` (ฝัง woff2 เป็น data URI ตามข้อ 23) ถูกสร้างไว้ตั้งแต่ M1 แต่ **ไม่มีที่ไหน link** (`pages.tsx` `<head>` มีแค่ app.css + content.css) · CLI preview ใส่ให้อยู่แล้ว (`cli/src/index.ts` เงื่อนไข `fragment.includes("katex")`) → ฝั่ง server ตกหล่น · อาการที่ยืนยันจากเบราว์เซอร์จริง: `.katex-display` computed `display: inline` (katex.css กำหนด `block`) → สมการ display ไม่ขึ้นบรรทัดใหม่/ไม่จัดกลาง และ **ข้อความ MathML ดิบโผล่ซ้ำข้างสูตร** (`E = mc² E = mc2 และ:`) เพราะ `.katex-mathml { clip-path: inset(50%); position: absolute }` ไม่ถูกโหลด · **เคาะ**: `<link>` มาจาก SSR แบบมีเงื่อนไข (`Layout` รับ `math` = `fragment.includes("katex")`) + ฝั่ง client `ensureKatexCss()` หลัง repaint — เพราะผู้ใช้อาจ **พิมพ์สมการใหม่ตอนโหมดเขียน** (เนื้อหามีสมการแต่หน้าโหลดมาโดยไม่มี CSS) · ใช้ `data-katex` กันใส่ซ้ำ · **ไม่โหลดถาวรทุกหน้า** เพราะ ~380KB (ฟอนต์ฝัง base64) · CSP เดิมรองรับอยู่แล้ว (`font-src 'self' data:`) · regression: `packages/server/test/katex.test.ts` + browser check ใน `bun run shot` (มี `.katex` แล้ว `.katex-mathml` ต้อง `position: absolute`) |
| 71 | **soft line break → `<br>`** | เจอบั๊กจริงจากการใช้งาน: พิมพ์สองบรรทัดในย่อหน้าเดียว (`กินอะไรดีครับ\nควรจะนอน…`) แล้วอ่านเห็นเป็น **บรรทัดเดียว** · สาเหตุ: CommonMark กำหนดบรรทัดใหม่เดี่ยวในย่อหน้า = *soft break* → `remark-rehype` ปล่อยเป็น `\n` แล้ว HTML ยุบเป็นช่องว่าง · และ **โหมดเขียน (CM6) แสดงต้นฉบับเป็นบรรทัด** → สิ่งที่เห็นตอนพิมพ์ไม่ตรงกับตอนอ่าน (ขัดเจตนาข้อ 52/64 ที่ยึด "อ่านกับเขียนตรงกัน") · **เคาะ**: เพิ่ม `plugins/breaks.ts` ของ core เอง (ไม่เพิ่ม dependency — pattern เดียวกับ `mark.ts`/`wikilink.ts`) แยก `text` node ที่มี `\n` เป็น `text`/`break` → `remark-rehype` map เป็น `<br>` · วาง**หลัง** `remarkMark`/`remarkWikilinks` (syntax ที่พาดบรรทัดยังจับคู่ได้ก่อน) · **ไม่แตะ** `code` · `inlineCode` · `math` (ข้อความอยู่ใน `value` ไม่ใช่ `text` node) · **ต้องแก้ `mdastText()`** ให้ `break` คืน `"\n"` — ไม่งั้น block ที่อ่านทีละบรรทัด (`kv` · `steps` · `tabs`) จะยุบแถวติดกัน · regression: `packages/core/test/render.test.ts` (soft break · code block ไม่โดน · kv ยังแยกแถว) |
| 72 | **M3.5 — hub browse: นับจำนวน · ขอบเขต `?sort=` · ชื่อซ้ำ** | นับจำนวนใน section โฟลเดอร์ = เอกสาร **โดยตรง** (`dirnameOf(id) === path` — ตรงกับ `FolderPage.SubfolderRow`; badge นับ = แถวที่โชว์ ไม่โกง; โฟลเดอร์ที่มีของลึกกว่าคง header ค่า 0 ไว้ให้เข้าถึงได้) · **`?sort=` ครอบเฉพาะ flat list "ทั้งหมด"** — section อื่นมี order เอง (ปักหมุด pin-first · ล่าสุด mtime · โฟลเดอร์ใช้ `folderRowOrder` คง `order` ตรง sidebar; ถ้าให้ sort ทุก section จะทิ้ง `order` ตอน default) — sort control โชว์คู่กับ list ที่มันเรียง (โฟลเดอร์ว่าง = ไม่มี control ไม่มี chrome ตาย) · **ชื่อซ้ำ `x/` + `x.md` = warning `folder_file_name_clash`** ใน `doku check` (level warning → exit 0) และ `/d/x` เปิดเอกสารเสมอ (doc wins — ticket 06) |
| 73 | **build CSS ด้วย `bun x` ไม่ใช่ `bunx`** | เจอจาก gate แดง: `bun run build:css` fail `bunx: command not found` (exit 127) บนเครื่องที่มีแค่ binary `bun` → `packages/server/public/app.css` (gitignored) ไม่ถูก build → chrome/layout CSS 404 → **`bun run shot` a11y แดง 5 ข้อ** (1480px ×2 · 200% zoom ×2 · 916px@360) ทั้งที่ไม่มีบั๊กจริง — พิสูจน์ด้วย control: เอา app.css ออก → 5 issues กลับมาเป๊ะ, ใส่กลับ → เขียว · root element จริง = margin-note gutter hang (`blocks.ts:375`) + specimen `<pre>` ของ styleguide ที่ losing `overflow-auto` ตอน chrome CSS หาย — ไม่ใช่ content CSS ผิด · **เคาะ: ใช้ `bun x` (builtin ของ bun) ใน `package.json` + `scripts/dev.sh`** ไม่พึ่ง symlink นอก repo · **ข้อกำหนด: build ก่อนรัน gate** (`bun run build:css && bun run build:editor`) · follow-up ค้าง: `scripts/shot.ts` ยังไม่ครอบหน้า `/d/<folder>` (รอบนี้ห้ามแตะ gate — ปิดด้วย evidence แยก `var/s5-folder-shots.ts`) |
| 74 | asset filename charset (Q7) | **บังคับ charset ของ docs/06** `[a-zA-Z0-9._\-\u0E00-\u0E7F ]+` (latin + digits + `. _ -` + space + ไทย) กับ basename ของ asset ทุกไฟล์ที่ผ่าน REST — **upload `POST /api/docs/*path/assets`** validate ครบทุกไฟล์ในคำขอก่อนเขียนไฟล์แรก (ไม่ผ่าน = `400 path_invalid` + ไม่มีไฟล์หลุดลง disk) · **`DELETE /api/assets/*path`** ตรวจชื่อไฟล์เดียวกันก่อนแตะ fs (ไม่ผ่าน = `400 path_invalid`) · helper = `isSafeAssetName()` ที่ `packages/core/src/paths.ts` (isomorphic — CLI/MCP ใช้ร่วมกันได้) · mime allowlist ของ upload = `assetMimeOf()` ที่ `packages/core/src/assets.ts` ชุดเดียวกับ route serve (นอก allowlist = `400 asset_type_rejected` — ไม่กว้างกว่าที่ serve) · ไฟล์ที่เขียน vault ตรง ๆ (docs/05 §1) ยัง serve ได้ตามเดิม |

| 75 | **FTS5 = trigram tokenizer** | ค้น substring ได้ทั้งไทย/อังกฤษ **กลางประโยค** (unicode61 segment คำไทยไม่ได้) · query ที่สั้นกว่า 3 อักขระ = FTS ตอบ 0 แถวเสมอ → fallback LIKE เฉพาะ title/path/tags (**ไม่ linear-scan เนื้อหา** — ticket 04) · Bun มาพร้อม SQLite 3.53.2 ที่มี trigram แล้ว |
| 76 | **search engine อยู่ `@doku/fs-node`** | `var/index.db` เป็น var-side store ที่ server/CLI/MCP ใช้ร่วมกันแบบ `createNodeRevisionStore` — ห้ามวางใน `core` (isomorphic) และห้าม cli/server/mcp import กันเอง (plan §4 #3) · `server/src/index-db.ts` มีแค่ lifecycle (boot sync · watcher trailing-debounce 500ms · rescan 60s fallback ตาม docs/01 · พัง = stderr note ไม่ทำ server ล้ม) |
| 77 | **index migration = `PRAGMA user_version`** | ไม่ใช้ drizzle-kit — index disposable (docs/01): version ไม่ตรง = drop แล้ว rebuild จาก vault เท่านั้น · Drizzle ใช้กับ typed query ตาราง `docs`/`links` · FTS5 DDL = raw SQL (Drizzle ไม่ model virtual table) · WAL + busy_timeout = server เขียนพร้อม CLI/MCP อ่านได้ |
| 78 | **links เก็บ raw + resolve ตอน query** | ตาราง `links(from_id, kind, target)` ใน index (`kind` = `wiki`/`doc`/`dpath` รวม `/d/…`) · resolve ด้วย `buildDocIndex`/`resolveWikiTarget` ตอน `backlinks()` → ตรงกับ vault ปัจจุบันเสมอ · กำกวม = ตัวแรก (เหมือน render docs/01) · self-link ไม่นับเป็น backlink · ลบ/แก้ไฟล์ = sync แทนที่ทั้งชุด |
| 79 | **"หน้า search" ของ M5 = palette** | plan §5 (source of truth ของ sequence) บอก `GET /api/search` + palette upgrade → **ไม่สร้างหน้า `/search` เดี่ยว** (docs/07 ใช้คำว่า "หน้า search") · palette = fetch debounce 200ms + seq กันคำตอบเก่า + merge/dedup หน้าสุด (ค้นจากเนื้อหาได้ ไม่ใช่แค่ชื่อ/แท็ก) · อยากได้หน้าค้นเดี่ยว = follow-up หลัง v1 |
| 80 | **`doku build` ไม่ลบของเก่าใน `--out` + deploy เป็นโครง** | เขียนทับเฉพาะไฟล์ที่ generate เอง — **ไม่มีคำสั่งลบจาก CLI** · export = relative URL ทั้งหมด (`/d/x` → `x.html` · `/assets/…?h=` → ไฟล์จริง) + `index.html` (escape แล้ว) · Dockerfile/compose = โครงเท่านั้น (ข้อ 3 · CMD ต้อง build ทั้ง app.css และ editor.js ใน image เพราะ `public/` ถูก ignore) · backup = auto-git ใน `vault/.git` (dotfolder → watcher/tree/index ข้ามตาม invariant 9 · identity fallback เฉพาะ repo) |

> **ข้อ 56–61 = spec alignment ที่เจอตอน bug-hunt M3 — เคาะแล้ว แต่ยังไม่ implement**
> คิวอยู่ที่ [M3.1 Track F](07-roadmap.md) (เล็กและบางส่วนถูก M3.1 แตะอยู่แล้ว: `color` allowlist เกี่ยวกับ block control strip, KaTeX errorColor เกี่ยวกับงานสี)

## รอเคาะ

| # | เรื่อง | ทางเลือก / ข้อมูล |
|---|---|---|
