/**
 * CSS ชั้นเนื้อหาเอกสาร — มาจาก design system ที่ `@doku/core` (docs/08 ข้อ 28)
 *
 * ย้ายมารวมที่ core เพื่อให้ CLI preview และ server ใช้ชุดเดียวกันจริง ๆ
 * (เดิมมีสำเนาอยู่ทั้ง server และ cli → drift)
 */

export { CONTENT_CSS } from "@doku/core"
